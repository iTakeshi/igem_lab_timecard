%table.table
  %tr
    %th name
    %th status
    %th{colspan: 3} actions

  - @users.each do |u|
    %tr
      %td= u.name
      %td= status_with_color(u)
      %td= link_to('実験中', lab_user_path(u), class: 'btn btn-success') unless u.status == 1
      %td= link_to('離席', out_user_path(u), class: 'btn btn-warning') if u.status == 1
      %td= link_to('帰宅', home_user_path(u), class: 'btn btn-danger') unless u.status == 0
